# Informações detalhadas do Figma

## Estrutura da seção "contato"

### Layout mobile e tablete

contato__container

  contato__container--interno

    texto__container
      contato__titulo
      contato__texto
      
    form__container

      input__container--preenchido
        caixa (div__input sem uso)
          linha (border-bottom do campo de input)
          elementos__container (campo do input)
            textos__container (div dos textos)
              placelabel (texto do nome do campo reduzido)
              placeholder__container (div do placeholder)
                placeholder (texto do placeholder)
                retângulo (cursor)

      input__container--ativo
        caixa (div__input sem uso)
          linha (border-bottom do campo de input)
          elementos__container (campo do input)
            textos__container (div dos textos)
              placelabel (texto do nome do campo reduzido)
              placeholder__container (div do placeholder)
                placeholder (texto do placeholder)
                retângulo (cursor)

      input__container--embranco
      caixa (div__input sem uso)
        linha (border-bottom do campo de input)
        elementos__container (campo do input)
          placelabel (texto do nome do campo)

      input__container--textarea
      caixa (div__input sem uso)
        linha (border-bottom do campo de input)
        elementos__container (campo do input)
          placelabel (texto do nome do campo)

    botao__original
      texto

### Layout desktop

contato__container

  contato__container--interno

    contato__imagem (apenas esta alteração)

    contato__container--interno2 (apenas esta alteração)

      texto__container
        contato__titulo
        contato__texto
        
      form__container

        input__container--preenchido
          caixa (div__input sem uso)
            linha (border-bottom do campo de input)
            elementos__container (campo do input)
              textos__container (div dos textos)
                placelabel (texto do nome do campo reduzido)
                placeholder__container (div do placeholder)
                  placeholder (texto do placeholder)
                  retângulo (cursor)

        input__container--ativo
          caixa (div__input sem uso)
            linha (border-bottom do campo de input)
            elementos__container (campo do input)
              textos__container (div dos textos)
                placelabel (texto do nome do campo reduzido)
                placeholder__container (div do placeholder)
                  placeholder (texto do placeholder)
                  retângulo (cursor)

        input__container--embranco
        caixa (div__input sem uso)
          linha (border-bottom do campo de input)
          elementos__container (campo do input)
            placelabel (texto do nome do campo)

        input__container--textarea
        caixa (div__input sem uso)
          linha (border-bottom do campo de input)
          elementos__container (campo do input)
            placelabel (texto do nome do campo)

    botao__original
      texto


## Propriedades dos elementos

### Layout mobile

.contato__container{
  background: #EAF2FD; /* Azul/10% */

  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 32px;

  gap: 10px;

  width: 360px;
  height: 675px;
}

### layout tablete
.contato__container{
  width: 768px;
  height: 637px;
}

### layout desktop
.contato__container{
  padding: 64px;

  width: 1440px;
  height: 788px;
}

### Layout mobile

.contato__container--interno {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  padding: 0px;
  gap: 32px; /* espaço entre elementos */

  width: 296px;
  height: 611px;
}

### Tablete

.contato__container--interno{
  width: 704px;
  height: 573px;
}

### Layout mobile

.texto__container{
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  padding: 0px;
  gap: 8px;

  width: 296px;
  height: 110px;  
}

### Layout mobile

.contato__titulo{
  width: 85px;
  height: 26px;

  font-family: 'Raleway';
  font-style: normal;
  font-weight: 700;
  font-size: 22px;
  line-height: 26px;

  color: #464646; /* Preto/100% */
}

### Layout mobile

.contato__texto {
  width: 296px;
  height: 76px;

  font-family: 'Raleway';
  font-style: normal;
  font-weight: 500;
  font-size: 16px;
  line-height: 19px;

  color: #464646; /* Preto/100% */
}


/* Configuração dos campos do formulário */

### Layout mobile

.form__container {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  marging: 2rem 0;
  padding: 0px;
  gap: 32px; /* espaço entre elementos */

  width: 296px;
  height: 397px;
}

/* div de cada input */
/* 0.5rem entre cada campo de input */

### Layout mobile

.input__container { 
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  padding: 0px;
  gap: 8px;

  width: 296px;
  height: 56px;

  background: #FFFFFF;
  border-radius: 4px;
}

### Layout mobile

.input__container--textarea{
  height: 133px;
}

### Layout mobile

/* linha em baixo preenchido e embranco */
.border__botton {
  position: absolute;
  height: 1px;
  left: 0%;
  right: 0%;
  top: 55px;

  background: #C8C8C8; /* Preto/30% */
}

### Layout mobile

/* linha em baixo campo ativo */
.border__botton--ativo{
  height: 2px;

  background: #2A7AE4; /* Azul/100% */
}

### Layout mobile

/* div interna de cada input */
.elementos__container {
  display: flex;
  flex-direction: row;
  align-items: center;
  padding: 0px 12px;

  position: absolute;
  height: 40px;
  left: 0%;
  right: 0%;
  top: 8px;  
}

### Layout mobile

/* div interna de cada input em branco */
.elementos__container--embranco{
  height: 24px;
  top: 16px;
}

### Layout mobile

/* div interna textos do input (preenchido/ativo) */
.textos__container {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  padding: 0px;
  gap: 4px;

  width: 272px;
  height: 40px;  
}

### Layout mobile

/* div interna textos do input (embranco) */
.textos__container--embranco{
  display: none;
}

### Layout mobile

/* texto do nome do input (preenchido) */
placelabel--preenchido {
  font-family: 'Raleway';
  font-style: normal;
  font-weight: 400;
  font-size: 12px;
  line-height: 16px;
  
  display: flex;
  align-items: center;

  width: 272px;
  height: 16px;

  color: #A2A2A2; /* Preto/50% */  
}

### Layout mobile

/* container do conteúdo do input (preenchido) */
.placeholder__container {
  display: flex;
  flex-direction: row;
  align-items: center;
  padding: 0px;
  gap: 4px;

  width: 272px;
  height: 20px;  
}

### Layout mobile

/* texto - contaúdo do input preenchido */
.placeholder__texto {
  width: 98px;
  height: 20px;

  font-family: 'Raleway';
  font-style: normal;
  font-weight: 400;
  font-size: 16px;
  line-height: 20px;

  display: flex;
  align-items: center;

  color: #464646; /* Preto/100% */  
}

### Layout mobile

/* cursor do input preenchido */
.placeholder__cursor {
  width: 1px;
  height: 20px;

  background: #C8C8C8; /* Preto/30% */
  opacity: 0;
}






